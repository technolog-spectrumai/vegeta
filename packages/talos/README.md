# Talos

Standalone linear static structural analysis: any STEP file + explicit material, regions, supports
and loads → Gmsh mesh → CalculiX. Native files (`mesh.msh`, `model.inp`, `.frd`, `.dat`, logs) are
kept next to a `summary.json`. See `../../docs/talos.md`.
