# vegeta-ai

A Claude connector for iterating on Dedalus (CadQuery) designs. Claude proposes a new version of the
design source or new parameter values from your instruction; Vegeta builds and measures the proposal,
shows the diff, and only writes it when you accept. Nothing is changed silently.
See `../docs/ai.md`.
